<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base theme is intentionally minimal: Compose owns all styling at runtime.
         We only need a Material 3 parent so the platform decor behaves. -->
    <style name="Theme.BleSpam" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <!-- Splash screen using the AndroidX SplashScreen API. -->
    <style name="Theme.BleSpam.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_logo</item>
        <item name="postSplashScreenTheme">@style/Theme.BleSpam</item>
    </style>

</resources>
